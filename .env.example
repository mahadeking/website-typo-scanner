# Copy this file to .env and replace the placeholder with your API key.
OPENAI_API_KEY=your_openai_api_key_here

# Optional: override the default model used by typo_scanner.py.
OPENAI_MODEL=gpt-5.4-mini

# Recommended when the app is shared online.
APP_PASSWORD=choose_a_private_access_password
